import 'package:flutter/material.dart';

appbar({tittle}){
  return AppBar(
        title: Text("MtalK",
        style: TextStyle(
          fontSize: 30,
          
        ),
        ),
      );
}